<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="android:buttonStyle">@style/Base.Widget.AppCompat.Button.Borderless.ButtonStyles
        </item>
    </style>

    <style name="Base.Widget.AppCompat.Button.Borderless.ButtonStyles">
        <item name="android:width">@dimen/button_width</item>
        <item name="android:background">@color/translucent_teal</item>

    </style>

</resources>
